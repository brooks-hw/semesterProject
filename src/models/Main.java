package models;

import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        JFrame frame = new JFrame("Investor Buddy"); // Create JFrame with a title
        frame.setSize(900, 600); // Set size to 300x200
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); // Close on exit
        frame.setVisible(true);
    }
}
